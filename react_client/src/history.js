import { createBrowserHistory } from "history";

export default createBrowserHistory();
// Creo el browser history object yo misma, para usar PlainRouter, no BrowserRouter!!
// Esto es todo. Con esto, puedo usar el history en cualquier componente dentro del proyecto!!
